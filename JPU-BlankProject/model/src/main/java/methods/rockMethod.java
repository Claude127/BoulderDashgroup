package methods;

import model.mobile.IMobile;
import model.model.IMap;

public class rockMethod extends fallingMethods {
	@Override
	public void followMethods(IMobile concernedCharacter0, IMap map) {
		super.followMethods(concernedCharacter0, map);
	}
}
